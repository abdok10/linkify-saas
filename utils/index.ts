// constants
import {
  LIST_ITEM_VARIANTS,
  CHILD_VARIANTS,
  FADE_IN_VARIANTS,
  MODAL_VARIANTS,
} from "@/utils/constants/animations";
import { APP_DOMAIN, APP_HOSTNAMES, APP_NAME } from "@/utils/constants/site";
import {
  DEFAULT_AVATAR_URL,
  PAGINATION_LIMIT,
  COMPANIES,
  PROCESS,
} from "@/utils/constants/misc";
import {
  PLANS,
  PRICING_FEATURES,
  WORKSPACE_LIMIT,
} from "@/utils/constants/pricing";
import { NAV_LINKS } from "@/utils/constants/nav-links";
// import { aeonik, inter } from "@/utils/constants/fonts";

// functions
import { cn } from "@/utils/functions/cn";
import { isValidUrl } from "@/utils/functions/urls";
import { generateMetadata } from "@/utils/functions/metadata";

export {
  // constants
  LIST_ITEM_VARIANTS,
  CHILD_VARIANTS,
  APP_DOMAIN,
  APP_HOSTNAMES,
  APP_NAME,
  DEFAULT_AVATAR_URL,
  FADE_IN_VARIANTS,
  MODAL_VARIANTS,
  PAGINATION_LIMIT,
  PLANS,
  PRICING_FEATURES,
  WORKSPACE_LIMIT,
  NAV_LINKS,
  COMPANIES,
  PROCESS,
//   aeonik,
//   inter,

  // functions
  cn,
  isValidUrl,
  generateMetadata,
};
